/*
    retailer list controller
*/
(function (module) {
    // inject the controller params
    ctrl.$inject = ['$scope'];

    // create controller
    module.controller('retailerListController', ctrl);

    // controller body
    function ctrl($scope) {

        // page init
        init();

        function init() {
            
        }

        function bindRetailerGrid() {

        }
    }
})(angular.module('gbmono'));